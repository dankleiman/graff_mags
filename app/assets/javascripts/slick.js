$(document).ready(function(){
  $('.single-item').slick();
  // $('.fade').slick({
  // dots: true,
  // infinite: true,
  // speed: 500,
  // fade: true,
  // slide: '> div',
  // cssEase: 'linear'
  // });
  console.log('slick is ready to go');
});
